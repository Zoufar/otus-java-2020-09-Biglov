

Все сборщики прогонялись на одной программе с Xms400m -Xmx400m, int gotOut = 4 * 1000 * 197 (эта переменная регулирует скорость утекания), Thread.sleep(100).

В строку вывода по событиям добавлено количество итераций, пройденных во внешнем цикле генератора мусора (как показатель полезной производительности ).

С периодом 10 с по каждому сборщику фиксировались количество сборок и суммы длительности за 10 с (pure) и скользящее за 25 с (slipping). 
Таблица в конце соответствующего файла вывода (output_parallel_400_197c_325s.txt.md, output_serial_400_197c_239s.txt.md, output_gc1_400_197c_188s.txt.md соответственно).

результаты:
1  ParallelGC
общее время работы - 325 с
финальное количество итераций - 1014
начиная с 90-й секунды скользящее накопление длительности STW за 25 с становится примерно 50% интервала -  12 с.
к этому моменту количество итераций - 702.
последние 100 итераций проходятся за 150 с

2 SerialGC
общее время работы - 239 c
финальное количество итераций - 1089
начиная со 110-й секунды скользящее накопление длительности STW за 25 с примерно  50% интервала -  12 с.
к этому моменту количество итераций - 795.
последние 100 итераций проходятся за 60 с

3 G1
общее время работы - 188 c
финальное количество итераций - 1089
начиная со 150-й секунды скользящее накопление длительности STW за 25 с примерно  50% интервала -  13 с.
к этому моменту количество итераций - 992.
последние 100 итераций проходятся за 30 с


Вывод: с точки зрения осуществления полезной деятельности (количество итераций) - результаты в пределах погрешности.

G1, как я уже отмечал, выглядит "более живым" на последнем этапе деятельности при недостатке свободной памяти. Соответственно, когда память окончательно заканчивается - быстрее выдает исключение и заканчивает работу.

ParallelGC - последние 50 с вообще непонятно чем занимается. За это время проходит только одна итерация.



java -Xms400m -Xmx400m -XX:+UseParallelGC -verbose:gc -Xlog:gc*:file=./logs/gc.log -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=./logs/dump -jar hw08_gc-GCTests-0.1.jar
java -Xms400m -Xmx400m -XX:+UseSerialGC -verbose:gc -Xlog:gc*:file=./logs/gc.log -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=./logs/dump -jar hw08_gc-GCTests-0.1.jar
java -Xms400m -Xmx400m -XX:+UseG1GC -verbose:gc -Xlog:gc*:file=./logs/gc.log -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=./logs/dump -jar hw08_gc-GCTests-0.1.jar
